spring:
  main:
    banner-mode: off
  datasource:
    url: jdbc:postgresql://localhost:5499/escuela_db
    username: julianfas20
    password: PKey187364
    driver-class-name: org.postgresql.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

logging:
  pattern:
    dateformat: "hh:mm"
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE